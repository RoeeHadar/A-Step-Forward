{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @asf/web build",
  "outputDirectory": "apps/web/.next",
  "env": {
    "NEXT_PUBLIC_APP_NAME": "A Step Forward"
  }
}
