# Obsidian workspace (user-specific — not committed)
workspace.json
workspace-mobile.json
.mcp-startup.log

# Obsidian cache
.trash/

# Plugin data that varies by machine (keep community-plugins.json committed if added)
plugins/obsidian-git/data.json
