# Iteration 3

- **Started:** 2026-07-21
- **Variant:** `building` (locked)
- **Env:** production (B11 observe); fixes local — need deploy for B03 + B11 end-to-end retest
- **Scope:** Fix mock-exam RSC; isolated QA-B11; advance-on-pass wiring

## Checklist

- [x] Coordinator opened iter 3 (from iter-2 Deliberation)
- [x] Fix mock-exam: pass `examsById` JSON, not `getExam` function
- [x] Isolated QA-B11 UI (no Integration reseed)
- [x] Root-cause + fix: gate pass must call `advanceRollingPlanWindow` (+ `/app` + `/app/plan`)
- [ ] Deploy fixes → prod retest QA-B03 + full B11 (seed → pass → week 2 active without manual SQL)
- [ ] Deliberation
- [ ] Coordinator decision

## Isolated QA-B11 findings

1. Re-seeded `building` (`959a0461-…`, week1 `d19774db-…` active).
2. Live gate: 4/4 open answers → **94%**, `passed=true`.
3. After submit + hard-refresh `/app/plan`:
   - Neon: week1 **`completed`**, week2 still **`upcoming`**, `plan_last_adjusted_at=null`
   - UI: featured week2 status **עתידי** (no active week) — **FAIL on advance**
4. Root cause: `markWeekCompleted` alone; `/app/plan` (and `/app`) never called `advanceRollingPlanWindow` (only `GET /api/plans/current` did).
5. Diagnosis proof: manual promote week2 → active → UI shows **שבוע 2 · פעיל**, week1 הושלם, banner "התוכנית עודכנה לפי ההתקדמות שלך".

## Code fixes (local, undeployed)

| File | Change |
|------|--------|
| `mock-exam/page.tsx` + `mock-exam-client.tsx` | Serializable `examsById` (fixes QA-B03 RSC) |
| `weekly-quiz.ts` | After mark complete, call `advanceRollingPlanWindow`; `plan_adapted` = rolled |
| `app/plan/page.tsx`, `app/page.tsx` | Call `advanceRollingPlanWindow` before `getCurrentPlan` |

## Blocker for close

**Deploy to production**, then re-seed `building` and re-run B03 + B11 end-to-end. Do not claim PASS until that.

---

seed_variant: building  
round_id: 2026-07-21-adr0010-building
