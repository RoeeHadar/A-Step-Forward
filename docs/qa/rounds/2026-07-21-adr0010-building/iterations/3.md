# Iteration 3

- **Started:** 2026-07-21
- **Closed:** 2026-07-21 (prod retest after deploy `0b61077b`)
- **Variant:** `building` (locked)
- **Env:** production

## Checklist

- [x] Coordinator opened iter 3
- [x] Fix mock-exam RSC (`examsById`)
- [x] Fix gate advance (`advanceRollingPlanWindow` on submit + `/app` + `/app/plan`)
- [x] Deploy + verify (`scripts/verify-deploy.ps1` → SUCCESS)
- [x] QA-B03 prod retest — **PASS** (catalog + exam detail render)
- [x] Isolated QA-B11 — **PASS** (93% pass → week1 completed, week2 active, week4 appended; UI פעיל + plan-adapted cue)
- [ ] Deliberation
- [ ] Coordinator decision

## Deploy

- Commit: `0b61077b` `fix(frontend): mock-exam RSC + advance plan after gate pass`
- GitHub Actions + Vercel: **success**; smoke `/`, `/sign-in`, `/learn` 200

## Prod retest notes

### QA-B03
- `/app/quiz/mock-exam` lists exams; opening 5pt mock shows sections + התחל מבחן (no RSC crash).

### QA-B11
1. Re-seed `building` (`ce19361e-…`, week1 `9a2eda65-…`).
2. First attempt **66%** fail → week1 stayed `active` (correct no-advance).
3. Retake with detailed answers → **93%** / `passed=true` / `plan_adapted` cue on results.
4. Neon: week1 `completed`, week2 `active`, new week4 `upcoming` (roll window ran).
5. `/app/plan`: שבוע 2 · פעיל; week1 הושלם; banner "התוכנית עודכנה לפי ההתקדמות שלך".

---

seed_variant: building  
round_id: 2026-07-21-adr0010-building
