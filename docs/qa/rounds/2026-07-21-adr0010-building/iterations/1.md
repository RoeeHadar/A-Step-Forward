# Iteration 1

- **Started:** 2026-07-21
- **Variant:** `building` (charter-locked)
- **Phase:** Execute authorized (after plan+report + Deliberation)

## Checklist

- [x] integration.md
- [x] ui.md
- [x] qa.md
- [x] security.md
- [x] evals.md
- [x] deliberation.md (advocates + facilitator)
- [x] Coordinator decision
- [x] Execute: Product QA Scripts 1–6 (P0) — see `qa.md` execute log (4 PASS / 2 BLOCKED; no FAIL)
- [x] Execute: Integration local + Neon-gated checks — Vitest PASS; Neon HTTP BLOCKED (see integration.md)
- [x] Parallel: Security F1/F2/F4 track (F4 fixed locally; F1/F2 NEEDS-PR — see security.md)
- [x] Re-attest `building` after execute — QA live seed PASS; Integration Neon SELECT failed
- [x] Post-execute Deliberation — see `deliberation-execute-1.md` (non-binding; unanimous-clean: no)
- [x] Iteration 1 close → escalate env blockers; iteration 2 armed (not started)

## Coordinator decision (binding) — plan phase

Heard [Deliberation](../deliberation.md). **Unanimous-clean: no.** Variant lock OK.

1. **Authorize local execute** of Product QA Scripts **1–6** (QA-B02, B03, B09, B10, B11, B12) after live `building` attestation (`seed-pilot-demo.mjs --variant building` + fingerprint).
2. **Authorize Integration** checks that need no secrets first; Neon/HTTP gate + lesson≠advance next when `DATABASE_URL` available — do not treat FastAPI Phase-1 green as pilot coverage.
3. **Parallel Security track** (does not block Scripts 1–6): F1 custom-quiz client keys, F2 web chat SafetyModeration, F4 `markWeekCompleted` learner join — investigate + propose/fix with review-security on any PR.
4. **Downgrade** remediation-chip / taste gaps — non-blocking for clean criteria.
5. **Keep** `seed_variant: building`. Onboarding/`fresh` needs a secondary account — do not flip charter mid-round.
6. **No baseline promote.** Evals remain gap-tracking.
7. Circuit breaker unchanged: max **4** iterations.

## Coordinator decision (binding) — post-execute

Heard [`deliberation-execute-1.md`](../deliberation-execute-1.md). **Unanimous-clean: still no.**

1. **Close iteration 1** as partial success (4/6 QA P0 PASS; units green; F4 fixed).
2. **Escalate for intervention** (env, not product bugs): Clerk signed-in session for `/app` (unblock B03/B11) + Neon HTTP connectivity for Integration #8–16 (`fetch failed` despite QA seed working — reconcile which `DATABASE_URL`/TLS path is valid).
3. **Arm iteration 2** (do not start until unblocked): scope = QA-B03, QA-B11 + Integration #8–11/#16 only; keep `building`; ship F4 via PR + `review-security`; F1/F2 remain parallel NEEDS-PR.
4. **Do not** run another plan-only loop.
5. Iteration count toward circuit breaker: **1 of 4** consumed.

## Deliberation snapshot (non-binding; for history)

- Shared P0 spine: readiness mock-cap, mock ungate, lessons≠advance, critical-floor, gate pass→advance, retake rotation
- Conflict resolved by Coordinator: weekly-gate spine + parallel security (not serialize on F1 alone)

## Post-execute deliberation snapshot (non-binding)

- Heard [`deliberation-execute-1.md`](../deliberation-execute-1.md). **Unanimous-clean: no.** Variant lock OK (`building`).
- PASSED: QA-B02/B09/B10/B12; Integration units/lint; Security F4 fixed locally.
- BLOCKED: QA-B03/B11 (Clerk UI); Integration Neon HTTP #8–16; F1/F2 NEEDS-PR; UI/Evals still plan-only.
- Facilitator suggest: escalate Clerk session + Neon connectivity, then open iteration 2 (not another plan-only loop).

---

seed_variant: building  
round_id: 2026-07-21-adr0010-building
