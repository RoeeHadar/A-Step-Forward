# Sessions — 2026-07-19

Goal-paced adaptive planning rebuild (ADR-0009). Working iteratively through the
phased streams, shipping to `main` at each section end. **A separate agent is
rewriting lessons concurrently** — this work depends only on stable IDs
(`concept_id` + canonical skill atoms), never on lesson bodies / `lesson_skill_atoms`
/ `agent_hints`.

## Design decisions (grilling + resolution)

- **Objective:** maximize P(goal by deadline); plan is living/non-deterministic;
  rolling 2-week horizon; overflow when early, re-pace when behind.
- **Engine:** hybrid — fast deterministic skeleton + async LLM enrichment off the
  critical path.
- **Pacing:** derived per-goal frontier manifest; `required_velocity = remaining ÷
  weeks_left`; `weekly_load = min(capacity, required)`; at-risk when required > capacity.
- **Personalization:** attributes control HOW (session length, load, kind mix, tone),
  not WHAT (selection stays mastery + goal-frontier driven).
- **Potential = adaptive ambition:** core + stretch frontier; ahead → raise ambition,
  behind → protect core. Executed via generated practice (lessons are static).
- **Gate:** competency quiz per week; fail → remediation → soft-override (advance but
  `shaky` + spaced review; blocking only if goal-critical). Threshold relaxes for
  non-critical under deadline pressure.
- **Tests archive:** past tests + learner answers + Reviewer feedback.
- **Spaced review:** ~20–30% weekly capacity via existing Coach FSRS queue.
- Full rationale + resolved parameters in `docs/adr/0009-goal-paced-adaptive-planning.md`.

## Shipments

### Stream 1 — goal-frontier manifest + coverage gate (curriculum)
- `scripts/build-goal-frontiers.mjs` — derives the frontier from the KG
  (`points_levels` + subject + terminal spine, topo-ordered; `depth`, `downstream`,
  `critical`). University tracks scoped tighter than the quiz allowlist (calc1 ≠ la).
- `apps/web/src/lib/goal-frontiers.generated.json` — 8 goals, all terminals present.
- `apps/web/src/lib/goal-frontiers.overrides.json` — thin optional override layer (empty).
- `apps/web/src/lib/goal-frontiers.test.ts` — 58-assertion CI coverage gate. Green
  (test + typecheck + lint).
- Shipped `main` `c72cc2b5`; CI 4/4 green; Vercel deploy `success`.
- NOTE: `Invoke-WebRequest` hangs in this shell (ignores `-TimeoutSec`); use the
  Vercel deployment-status API + green CI as the authoritative deploy check.

### Stream 2 — deterministic pacing engine (planner)
- `apps/web/src/lib/plan-pacing.ts` — pure engine, imports ONLY the manifest (safe
  on onboarding path). remaining scope / required_velocity / capacity (hours +
  attention) / weekly_load / status (ahead|on_track|at_risk) / goal_readiness /
  next_concepts (foundations-first, unmastered).
- `apps/web/src/lib/plan-pacing.test.ts` — 17 unit tests. Green (test+typecheck+lint).
- Not yet wired to a route (pure lib; no prod behavior change).
