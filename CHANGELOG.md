# Changelog

All notable changes to this project are documented here. The format is based on
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this project adheres
to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). Entries are
auto-generated from [Conventional Commits](https://www.conventionalcommits.org)
by `git-cliff` (see `cliff.toml` and `.github/workflows/changelog.yml`).

## [unreleased]

### Features
- **graphrag:** ontology migration, ingestion pipeline + seed corpus, retrieval
  modes with Neo4j service, KG retrieval evals wired into the CI runner.
- **frontend:** marketing landing, Clerk auth pages, learner dashboard, streaming
  agent chat, lesson view, Memory Inspector, progress dashboards, educator and
  admin RBAC dashboards.
- **schemas/ui:** shared `@asf/schemas` (Zod) and `@asf/ui` (shadcn primitives)
  workspace packages.

### Evals
- **safety:** `safety_moderation` capability + safety promptfoo matrices.

### Docs
- Round 2 resume briefs for all sub-agent streams + Release Captain brief.

### CI / Build
- Deploy workflows for Vercel (`deploy-web`), Fly.io API (`deploy-api`), Fly.io
  workers (`deploy-workers`), and prod promotion (`deploy-prod`) gated on green
  evals.
- `lint-test` workflow runs Ruff + mypy + pytest (Python) and ESLint + tsc +
  vitest (web) on PRs.

<!-- generated by git-cliff (CI). Update via `git cliff -o CHANGELOG.md`. -->
